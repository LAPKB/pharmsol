[package]
name = "browser-compile-bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[workspace]

[dependencies]
base64 = "0.22.1"
pharmsol = { path = "../../..", default-features = false, features = ["dsl-wasm-compile"] }
serde_json = "1.0.117"